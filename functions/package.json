{
  "name": "ledger-functions",
  "private": true,
  "description": "Server-side piece of Ledger's Pin now feature: sends a Web Push notification the instant a ledger_pins document is created. Deployed separately from the static site (firebase deploy --only functions).",
  "engines": {
    "node": "20"
  },
  "main": "index.js",
  "dependencies": {
    "firebase-admin": "^12.6.0",
    "firebase-functions": "^5.1.1",
    "web-push": "^3.6.7"
  }
}
